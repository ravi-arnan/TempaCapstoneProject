# Asahlagi Backend Dependencies
# Python 3.11+ required
#
# DL inference (IndoT5) runs on Hugging Face Space (cloud), NOT locally.
# Backend talks to Space via httpx. If Space unavailable, backend falls
# back to rule-based generation (no torch/transformers needed for that).
#
# This keeps the backend lightweight (~450MB venv vs ~3GB with torch).

# ===== Web framework =====
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12  # multipart/form-data for PDF uploads

# ===== HTTP client (calls HF Space + URL fetching) =====
httpx==0.27.2

# ===== Config =====
python-dotenv==1.0.1   # loads backend/.env for HF_SPACE_URL etc.

# ===== Database (gamification — Neon Postgres) =====
sqlalchemy==2.0.36
psycopg[binary]==3.2.3   # psycopg v3 driver; [binary] avoids needing system libpq

# ===== Google login (OAuth — verify Google ID tokens) =====
google-auth==2.53.0    # verifies GIS ID tokens against Google's public keys
requests==2.32.3       # REQUIRED by google.auth.transport.requests — not pulled in transitively on a clean image

# ===== ML conv (sklearn) — used by Desta's classifier (lightweight, ~50MB) =====
scikit-learn==1.8.0
joblib==1.4.2
numpy==2.0.2
pandas==2.2.3

# ===== Source extraction (PDF + URL article) =====
pdfplumber==0.11.4     # PDF text & table extraction (handles multi-column layout well)
trafilatura==1.12.2    # Article text extraction from URLs

# ===== Rate limiting =====
slowapi==0.1.9

# ===== Dev / testing =====
pytest==8.3.3

# ===== NOT INSTALLED LOCALLY (DL inference is cloud-hosted) =====
# torch, transformers, sentencepiece — these live on the HF Space, not here.
# If you really want local DL fallback, install them manually:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install transformers==4.45.2 sentencepiece==0.2.0