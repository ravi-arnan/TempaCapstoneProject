# Asahlagi Backend Dependencies
# Python 3.11+ required

# Core framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Optional analytical tooling (per CLAUDE.md / PRD.md — only if Ariq needs them)
# pandas==2.2.3
# scikit-learn==1.5.2

# Dev / testing
pytest==8.3.3
httpx==0.27.2  # used by FastAPI's TestClient
